interface:
  display_name: "OCI IAM Administration"
  short_description: "Manage OCI IAM with least privilege"
  default_prompt: "Use $oci-iam-admin to plan this least-privilege OCI IAM change safely."
