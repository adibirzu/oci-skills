interface:
  display_name: "OCI Networking and Compute"
  short_description: "Operate OCI networks and compute safely"
  default_prompt: "Use $oci-networking-compute to plan this OCI network or compute operation safely."
